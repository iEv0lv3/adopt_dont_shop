<h1>Pets availabe at <%= "#{@shelter.name}" %>:</h1>

<%= @shelter.pets.each do |pet| %>
  <%= image_tag(pet.image) %>
  <p><%= pet.name %></p>
  <p><%= pet.age %></p>
  <p><%= pet.sex %></p>
<% end %>

<%= link_to 'Create Pet', "/shelters/#{@shelter.id}/pets/new" %>
